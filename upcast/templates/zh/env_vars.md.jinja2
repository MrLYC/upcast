{% extends "base.md.jinja2" %}

{% block summary %}
{{ super() }}
- **环境变量总数**: {{ summary.total_env_vars }}
- **必需变量**: {{ summary.required_count }}
- **可选变量**: {{ summary.optional_count }}
{% endblock %}

{% block results %}
{% for var_name, var_info in results.items() %}

### {{ var_name }}

**是否必需**: {{ '是' if var_info.required else '否' }}
{% if var_info.default_value %}
**默认值**: `{{ var_info.default_value }}`
{% endif %}

#### 使用位置

| 文件 | 行号 | 列号 | 模式 | 代码 |
|------|------|------|------|------|
{% for loc in var_info.locations %}
| {{ loc.file }} | {{ loc.line or '不适用' }} | {{ loc.column or '不适用' }} | {{ loc.pattern }} | {{ loc.code or '不适用' }} |
{% endfor %}

---

{% endfor %}
{% endblock %}
